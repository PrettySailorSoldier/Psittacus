# Psittacus
Extract text from video files and screen recordings using frame-by-frame OCR. The user drops a video file in, the app samples frames at a configurable interval, runs OCR on each frame, deduplicates repeated text across frames, and delivers a clean text output they can copy or export.
